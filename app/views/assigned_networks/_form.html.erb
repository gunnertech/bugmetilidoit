<%= simple_form_for(@assigned_network) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :network %>
    <%= f.association :assigned_task %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
